import { ContattoCreaPipe } from './contatto-crea.pipe';

describe('ContattoCreaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContattoCreaPipe();
    expect(pipe).toBeTruthy();
  });
});
