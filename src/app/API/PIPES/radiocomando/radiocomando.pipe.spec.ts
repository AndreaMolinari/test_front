import { RadiocomandoPipe } from './radiocomando.pipe';

describe('RadiocomandoPipe', () => {
  it('create an instance', () => {
    const pipe = new RadiocomandoPipe();
    expect(pipe).toBeTruthy();
  });
});
