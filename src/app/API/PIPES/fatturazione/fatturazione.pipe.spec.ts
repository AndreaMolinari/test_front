import { FatturazionePipe } from './fatturazione.pipe';

describe('FatturazionePipe', () => {
  it('create an instance', () => {
    const pipe = new FatturazionePipe();
    expect(pipe).toBeTruthy();
  });
});
