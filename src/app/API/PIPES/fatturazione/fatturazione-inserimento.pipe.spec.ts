import { FatturazioneInserimentoPipe } from './fatturazione-inserimento.pipe';

describe('FatturazioneInserimentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FatturazioneInserimentoPipe();
    expect(pipe).toBeTruthy();
  });
});
