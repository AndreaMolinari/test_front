import { ComponentePipe } from './componente.pipe';

describe('ComponentePipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentePipe();
    expect(pipe).toBeTruthy();
  });
});
