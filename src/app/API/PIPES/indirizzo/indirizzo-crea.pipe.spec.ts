import { IndirizzoCreaPipe } from './indirizzo-crea.pipe';

describe('IndirizzoCreaPipe', () => {
  it('create an instance', () => {
    const pipe = new IndirizzoCreaPipe();
    expect(pipe).toBeTruthy();
  });
});
