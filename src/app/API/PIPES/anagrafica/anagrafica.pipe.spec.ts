import { AnagraficaPipe } from './anagrafica.pipe';

describe('AnagraficaPipe', () => {
  it('create an instance', () => {
    const pipe = new AnagraficaPipe();
    expect(pipe).toBeTruthy();
  });
});
